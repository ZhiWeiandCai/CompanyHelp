<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xht.android.companyhelp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:icon="@null"
            android:logo="@mipmap/logo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".RegisterActivity"></activity>

        <provider
            android:name=".provider.DatabaseHelpProvider"
            android:authorities="com.xht.android.companyhelp.DatabaseHelpProvider"
            android:exported="false" />

        <activity android:name=".ZhuCeCompanyActivity"></activity>
        <activity android:name=".DaiLiJIZhangActivity"></activity>
        <activity android:name=".PayOptActivity"></activity>
    </application>

</manifest>